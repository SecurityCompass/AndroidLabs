<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
      package="com.securitycompass.labs.falsesecuremobile"
      android:versionCode="1"
      android:versionName="1.0">
    <application android:icon="@drawable/icon" android:label="@string/app_name" android:name=".BankingApplication">
        <activity android:name=".SummaryActivity"
                  android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        <activity android:name=".LoginActivity" android:label="@string/app_name"/>
        <activity android:name=".TransferActivity" android:label="@string/app_name"/>
        <activity android:name=".AccountsActivity" android:label="@string/app_name"/>
        <activity android:name=".StatementActivity" android:label="@string/app_name"/>
        <activity android:name=".SetLocalPasswordActivity" android:label="@string/app_name"/>
        <activity android:name=".SetServerCredentialsActivity" android:label="@string/app_name"/>
        <activity android:name=".ViewStatementActivity" android:label="@string/app_name"/>
        <activity android:name=".EditPreferencesActivity" android:label="@string/app_name"/>

    </application>
    
<uses-permission android:name="android.permission.INTERNET"/>
<uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

</manifest>